predictor/gin.singleton.constructor = @SvmRegressor
predictor = @predictor/gin.singleton()

SvmRegressor.params = {
    'C': 1,
    'kernel': 'rbf',
    'gamma': 'scale',
    }

# Include hyperparameter optimization config
include 'configs/predictors/regressors/optimization/svn_hyperparams.gin'